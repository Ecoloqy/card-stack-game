import { type ItemType } from './item-type';

export type IconItemType = LayoutIconType | ItemType;

export enum LayoutIconType {
    'FOOD' = 'FOOD',
    'CLOCK' = 'CLOCK',
    'CALENDAR' = 'CALENDAR'
}
